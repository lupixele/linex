# LinuxDroid ALSA-to-PulseAudio redirect
# Routes legacy ALSA audio streams into PulseAudio server
pcm.!default {
    type pulse
    hint {
        show on
        description "Default ALSA Output (via LinuxDroid PulseAudio)"
    }
}

ctl.!default {
    type pulse
}
